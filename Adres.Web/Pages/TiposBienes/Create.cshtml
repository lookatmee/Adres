@page
@model Adres.Web.Pages.TiposBienes.CreateModel
@{
    ViewData["Title"] = "Nuevo Tipo de Bien/Servicio";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="text-adres-primary"><i class="fas fa-boxes"></i> Nuevo Tipo de Bien/Servicio</h1>
    <a asp-page="Index" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Volver
    </a>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="card card-adres">
            <div class="card-body">
                <form method="post" id="createForm" class="needs-validation" novalidate>
                    <div class="mb-3">
                        <label asp-for="TipoBien.Descripcion" class="form-label">Descripción</label>
                        <input asp-for="TipoBien.Descripcion" class="form-control form-control-adres" required />
                        <span asp-validation-for="TipoBien.Descripcion" class="text-danger"></span>
                        <div class="invalid-feedback">
                            La descripción es requerida.
                        </div>
                    </div>
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-adres-primary">
                            <i class="fas fa-save"></i> Guardar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#createForm').on('submit', function(event) {
                if (!validateForm('createForm')) {
                    event.preventDefault();
                    event.stopPropagation();
                }
            });
        });
    </script>
} 